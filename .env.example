# ==================================================
# Epic Tech AI - Environment Variables Template
# ==================================================
#
# Instructions:
# 1. Rename this file to ".env" in your local development environment.
# 2. Fill in the actual values for your setup.
# 3. IMPORTANT: Add ".env" to your .gitignore file to prevent committing secrets.

# --- Application Security ---
# A strong, randomly generated string is required for JWT token signing.
# You can generate one using: openssl rand -hex 32
SECRET_KEY="a_very_secret_key_for_dev_only_please_change_me"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- Database Configuration ---
# The current demo uses an in-memory dictionary. When you connect a real
# database, you would use a URL like this.
# DATABASE_URL="postgresql://user:password@host:port/dbname"

# --- CORS (Cross-Origin Resource Sharing) ---
# A comma-separated list of origins that are allowed to make requests to the API.
# Use "*" for development only, but specify your frontend domain in production.
# Example: CORS_ORIGINS="http://localhost:3000,https://www.epictechai.com"
CORS_ORIGINS="*"

# --- API & Frontend URLs ---
# The public-facing URL of your backend API.
API_URL="http://localhost:8000"
